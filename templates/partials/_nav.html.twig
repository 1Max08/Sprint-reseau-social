<nav class="bg-primaryBlue shadow-md">
    <div class="container mx-auto px-4 py-3 flex flex-wrap items-center justify-between">
        <a class="text-white font-bold text-xl tracking-wide" href="#">Mini Reseau-Social</a>
        <button class="text-white block lg:hidden focus:outline-none">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                      d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
        </button>
        <div class="w-full lg:flex lg:items-center lg:w-auto hidden">
            <ul class="lg:flex lg:space-x-6 text-center">
                <li>
                    <a class="text-white hover:text-primaryGreen font-semibold transition-colors" href="/">Accueil</a>
                </li>
            </ul>
            <ul class="lg:flex lg:space-x-4 mt-3 lg:mt-0">
                {% if app.user %}
                <span class="text-white mx-2 font-bold">Bienvenue -</span>
                <li>
                    <a class="text-white hover:text-primaryGreen font-semibold transition-colors" href="/logout">Deconnexion</a>
                </li>
                <li>
                    <a class="text-white hover:text-primaryGreen font-semibold transition-colors" href="/create">Ajouter un livre</a>
                </li>
                {% else %}
                <li>
                    <a class="text-white hover:text-primaryGreen font-semibold transition-colors" href="/register">Inscription</a>
                </li>
                <li>
                    <a class="text-white hover:text-primaryGreen font-semibold transition-colors" href="/login">Connexion</a>
                </li>
                {% endif %}
            </ul>
            <button id="theme" class="bg-white dark:bg-brand-darkgray text-black dark:text-white px-2 py-1 rounded text-xs md:text-base">🌗</button>
        </div>
    </div>
</nav>
