<nav class="bg-primaryBlue shadow-md">
    <div class="container mx-auto px-4 py-3 flex items-center justify-between">
        <!-- Logo -->
        <a href="{{ path('default_home') }}" class="text-white font-bold text-xl">
            Mini Réseau-Social
        </a>

        <!-- Bouton burger -->
        <button id="menu-btn"
                class="text-white block lg:hidden focus:outline-none"
                aria-expanded="false"
                aria-controls="mobile-menu">
            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M4 6h16M4 12h16M4 18h16" />
            </svg>
        </button>

        <!-- Menu desktop -->
        <div class="hidden lg:flex lg:items-center lg:space-x-6">
            <a href="{{ path('default_home') }}" class="text-white hover:text-primaryGreen font-semibold transition">Accueil</a>

            {% if app.user %}
                <span class="text-white font-bold">Bienvenue {{ app.user.email }}</span>
                <a href="{{ path('messages_create') }}" class="text-white hover:text-primaryGreen font-semibold transition">Ajouter un post</a>
                <a href="{{ path('app_logout') }}" class="text-white hover:text-primaryGreen font-semibold transition">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_register') }}" class="text-white hover:text-primaryGreen font-semibold transition">Inscription</a>
                <a href="{{ path('app_login') }}" class="text-white hover:text-primaryGreen font-semibold transition">Connexion</a>
            {% endif %}

            <button id="theme"
                    class="bg-white dark:bg-gray-800 text-black dark:text-white px-2 py-1 rounded text-xs">
                🌗
            </button>
        </div>
    </div>

    <!-- Menu mobile -->
    <div id="mobile-menu" class="hidden bg-primaryBlue lg:hidden flex flex-col items-center py-4 space-y-3">
        <a href="{{ path('default_home') }}" class="text-white hover:text-primaryGreen font-semibold">Accueil</a>

        {% if app.user %}
            <a href="{{ path('messages_create') }}" class="text-white hover:text-primaryGreen font-semibold">Ajouter un post</a>
            <a href="{{ path('app_logout') }}" class="text-white hover:text-primaryGreen font-semibold">Déconnexion</a>
        {% else %}
            <a href="{{ path('app_register') }}" class="text-white hover:text-primaryGreen font-semibold">Inscription</a>
            <a href="{{ path('app_login') }}" class="text-white hover:text-primaryGreen font-semibold">Connexion</a>
        {% endif %}

        <button id="theme"
                class="bg-white dark:bg-gray-800 text-black dark:text-white px-3 py-1 rounded text-sm">
            🌗
        </button>
    </div>
</nav>
