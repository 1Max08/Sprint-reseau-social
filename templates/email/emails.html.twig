<h2>Nouveau message publié</h2>

<p>{{ message.content }}</p>

{% if message.image %}
    <img src="{{ asset(message.image) }}" alt="Image du message" style="max-width:300px;">
{% endif %}

<a href="{{ url('messages_message', {'id': message.id}) }}" style="color: #1D4ED8; text-decoration: underline;">
    Voir le message
</a>
